# 独立运行 Agent

当前家庭发布r90（2026-09-21 08:11，完整公开提交9697889）；本次使用升级前Cookie完成双入口验证，所有原未过期会话保留。普通升级不使用灾难恢复副本覆盖主库。

最终交付主体是独立成长陪伴Agent，包含运行服务、网页入口、业务工具与家庭数据。Hermes或其他Agent Harness仅是内部候选，外部宿主插件不能替代交付；LLM须能按配置更换。以下说明当前已实现的Python进程与调度方式，完整自主跟进和跨模型验收仍见[V1交付目标](V1交付目标.md)，不因存在这些进程就认定全部完成。

默认采用单机家庭中心：应用主机与消息电脑是同一台家庭设备，网页、Agent、主库、附件及采集器共用原有目录，打印桥访问本机服务；手机和其他电脑访问该设备的认证网页。下文“新Mac同机运行”是现有配置基础，认证手机入口与本地打印桥仍需另行配置和实测；后续Linux及远程采集说明保留给可选的分开部署，不是每家的必要步骤。家庭中心必须保持运行，休眠、关机或退出必要登录会话会影响可用性。

试用实例的网页、Agent、SQLite主库及附件已迁到一台家庭电脑；原远端仅保留可选认证HTTPS转发，旧后台已停并隔离。迁移前已停止旧写入并生成一致备份，在隔离目录核对业务数据、原件、同一实例HTTP、日历UID、模型配置及真实固定合成文字调用；选定打印机设备配置随备份保留，桥接token仍单独配置。当前手机、出纸、冷开机和全新Mac仍待验收。

Agent 和消息采集由操作系统定时运行，不需要打开 Codex 或网页。后台先处理已经保存的资料，家长打开网页时查看结果，并通过原有待办、记录和建议反馈入口跟进。网页展示属于站内提醒；本版本不提供微信、QQ、短信或浏览器后台推送，也不把生成结果记为通知已送达。

锁屏与睡眠分开验收：在电脑保持唤醒、用户仍登录的条件下，锁屏不应暂停网页、Agent或已获授权的CLI/API采集。微信文件读取超时不能自动归因为锁屏或权限拒绝；须核对实际后台进程、当轮结果及系统记录。QQ界面读取备选依赖可访问的聊天窗口，窗口不可用时保留缺口，不阻止其他后台工作，也不能以保持解锁代替非界面来源的持续运行验收。

Codex 用于开发、部署和排障；项目跟进任务只跟踪交付问题，不承担日常消息采集或到期分析。业务调度由上述系统进程执行，模型从家庭私有配置读取；模型配置与实际调用记录必须分别核对。采集器进程在线而消息接口不可用时，应明确标记来源停用或失败，不能记为采集成功。

官方 QQ 机器人可作为另一条接入候选，使用独立机器人身份与 AppID/AppSecret，无须用个人 QQ 登录。腾讯官方 SDK 已提供群内 @ 机器人事件；普通未 @ 消息、原始图片与历史范围必须按实际机器人权限逐项验证。未完成真实事件接收前，不将其列为已经可用的班级群采集器。参考 [腾讯 QQ 机器人 Python SDK](https://github.com/tencent-connect/botpy) 与 [官方群事件示例](https://github.com/tencent-connect/botpy/blob/master/examples/demo_group_reply_text.py)。

## 进程与资料归属

- 应用主机：运行 `app.py` 和 `family_agent.py --once`，主库始终留在这一台机器。Agent 读取已保存的来源、学习记录和家长反馈，按实际日期回看；模型输出是可核对的建议，不能自行认定孩子完成、掌握或获奖。
- 消息所在电脑：运行 `family_collect.py --config private/collector.json --interval 300`。微信复用这台机器上实际可用、已授权的 CLI；QQ 的现有 CLI 接口和已授权 Cua 保底路线见 V1 的 S02，Cua 已有实验性单次窗口片段路径，持续读取未验收，不能写入 qq_cli 冒充原生消息接口。个人登录态不复制到应用主机。
- 来源授权与游标由应用主机维护。采集器仅按后台允许的会话读取，后台确认接收后才能推进游标；采集器不改本地数据库镜像，不以空结果覆盖读取失败。
- 采集频率由 `/api/agent/collector` 按家庭时间（UTC+8）控制：11:00–14:00、16:00–22:00每30分钟，其余每60分钟；端点只下发到期的已启用来源。以已保存的 `last_attempt` 计算下次时刻，失败或缺少CLI但已登记结果也等待同样间隔，不拿计划下发冒充实际读取。首次无尝试记录的来源可读；停用状态和来源绑定不变。11:00、14:00、16:00、22:00按新时段计算，网页使用相同的下次时刻加5分钟轮询余量判断过时。既有采集器和旧配置字段无需更改；更新网页进程后生效，不为降频重启持久采集器。服务器或配置不可读时，采集器仅沿用每轮5分钟的状态检查，不伪造成功；若结果提交未获服务端保存，本次实际尝试尚无法用于服务端节流。
- 失败诊断仅显示已知原因：工具未接通、无法启动、响应超时、返回格式无法核对、QQ连续位置未衔接；未知返回使用通用核对失败，不透传命令输出、路径或凭据，也不推定系统权限原因。失败回执保留上次成功时间、游标与原消息。保存配置、读取成功、入库确认及模型处理是不同步骤；配置恢复由原长驻进程下轮重新加载，勿用新进程试读替代原后台宿主验收。
- Agent 的处理状态和产物保存于应用主机的私有 SQLite。原有采集电脑维护的 Markdown、`采集状态.json`、`陪伴建议.json` 和 `陪伴提醒状态.json` 继续保留各自归属，不把服务器新产物写回这些会被旧同步覆盖的文件。

当前实现没有额外引入 Agent 框架、向量库或消息队列服务。模型只连接本家庭配置的端点；模型未配置或离线时，已有记录、待办、文件导入和到期事项仍可查看处理，到期回看提醒不依赖模型。需要模型的工作保留在队列中，同一资料指纹自动尝试共三次：首次失败后等5分钟，再失败后等10分钟；第三次失败即暂停自动调用，重启后仍保持暂停。家长核对模型或资料后，可在网页助手状态中点“重试”开启下一轮；资料发生实际更正时产生新的指纹。等待重试的旧批次不会堵住新资料，每轮最多调用模型三次，不用编造内容填满建议区。只有有依据的新变化才更新结果，无回复不表示已采纳或已完成。

模型整理学校消息、生成单次学习建议及持续目标时，只能从本次提供的来源编号中选择引用；原因的支持/反证选项排除学校要求。服务端仍逐字核对原文，未知或被抄错的编号直接拒绝，不按相似度猜配。该约束不改动原始编号、已确认计划或失败重试上限。 为兼容不同模型的枚举限制，本次超过64个编号或编号总长超过2000字符时不扩展schema，仍保留全部原输入和既有服务端校验，不截掉学习证据。

学习目标直接从既有SQLite选取依据，不另调用模型检索：学习记录与关联任务反馈各最多24条，各保留最近6条，并优先纳入已确认方案引用、支持/反驳依据及学习记录明确关联的后续更正。超过范围和不可核对的引用分别提示，历史引用不代替当前原文；原引用失去可核对归属时，旧模型评估、候选原因和旧方案也不再进入模型输入，家长历史仍保留。学校原消息仍只取最近6条，全部已授权关联原文参与更正失效判断；这不表示模型读过全部学校历史。规则升级会使旧待审核建议失效，已确认计划保持。旧方案原文仅从匹配的已确认建议恢复，无需数据库迁移。

同一任务总尝试上限已实现。其余成本要求是按明确任务选择轻模型或默认模型、限制每项上下文与输出、给总尝试设限，并记录实际调用用量与升级原因；规则采集、去重与到期判断继续不调用模型。模型调用现先在原有私有SQLite登记，再发请求，记录任务、请求模型、服务返回的模型标识（若提供）、耗时和服务报告的输入／输出／总token；不记录正文或密钥。连接检查也计入，语音转写不在本项内。失败和中断保留，用量缺失记未知；收到结构化答复不代表业务核对或教学质量通过。家长在“家庭设置 → 模型连接 → 近30天模型用量”查看，刷新不会调用模型，记录随原数据库备份恢复，不回填过去的调用。当前仅纯文字学校通知筛选可用轻模型；规划、教学、查询、记录整理、日历草稿及图片仍用默认模型。学校通知筛选的结构化输出上限为6000，其他模型任务为3000；两者共用三次重试和每轮调用边界。模型同时给出“新增事项”和已有事项时保留为家长待核对草稿，未知事项编号仍拒绝，不用自动建项掩盖冲突。轻模型失败直接保留错误，不自动升级；不能将每轮三次当作整日或整项预算，也不用输出上限冒充实际token消耗。

## 家庭网页配置

手机日历由家长认证入口下的 `calendar.ics` 提供，只读取现有事件与明确的参与决定，不调用模型、不另建日程库、不开放匿名订阅。保留 HTTPS 与应用的 Basic 认证；手机日历需单独提供家庭服务凭据。事件 UID 使用配置的 `FAMILY_CALENDAR_ID`，未配置则使用 `FAMILY_HOST`，本地默认 `local-family-learning`；已订阅后迁移域名时，将旧值保留为 `FAMILY_CALENDAR_ID`，避免所有事件被视为新事件。该值是稳定标识，不是密码。

“说一句安排”使用现有模型配置和原日历保存接口，支持每日、周末、指定星期/月日与多时段。模型仅返回待核对草稿；日期以引用的原话和本次北京时间核验，额外时段编号由后台生成，保存重试保持同一编号。未知起点/星期/钟点不能变成今天或固定时刻，手动填写仍可独立使用。只有明确请求整理时调用模型，打开页面或日历不触发推理。

首版每个订阅最多1000条原始安排（重复系列及每笔逐次记录分别计一条），每个系列最多8个时段事件，保留历史与取消。每日、周末、指定星期/月日沿保存规则生成；没有所选月日时跳过，指定日以家庭时区计算。原系列与额外时段使用稳定标识；“处理本次”在同一calendar_events表保存一条独立单次记录，occurrence增量列保存原日期/时段、原说明和变更历史。订阅以EXDATE排除原时段，并保留该次单独UID，改期、完成或取消不会复制原日期。修改原规则保留已单独处理的记录；有逐次记录时拒绝把原循环改成单次。该增量随唯一SQLite主库备份，不能用旧镜像覆盖。超限或来源读取失败返回错误，不截断或用空日历覆盖。课表节次不导出，未知钟点作为透明的时间待定日期，原始消息与备注不进入订阅。协议和认证接口检查通过不等于真实 iPhone 已同步；首次认证与后续刷新需家庭实测。

应用已安装启动后，空家庭自动进入“家庭设置”；已有家庭从顶部进入。网页可以添加或更正孩子，填写微信／QQ来源的明确会话 ID、名称、孩子归属与启停；本轮不自动列举群。已有历史来源不可改绑或清空游标；停用保留既有信息。模型地址、模型名和可选密钥可以稍后填写，已有进程环境配置继续优先。

模型地址支持两种形式：Chat Completions填写API基础地址（例如`http://127.0.0.1:1234/v1`）；Responses填写以`/responses`结尾的完整接口地址（例如`https://ark.cn-beijing.volces.com/api/v3/responses`）。两者沿用相同模型名、密钥及业务校验，网页和独立Agent共用同一份配置。Responses发送`store=false`、不携带服务端会话编号或工具，读取完成的结构化输出；返回拒绝、未完成或意外工具结果时保留手动处理入口。此参数不替代服务商自己的日志和数据政策。未验证的推理参数留空，不跨服务商套用。

可选轻模型在网页“更多模型选项”中配置，私有 `model.json` 字段为 `light_model`，旧四字段文件仍可读取。环境部署使用 `FAMILY_LLM_LIGHT_MODEL`；任一模型配置环境变量存在时，整套模型配置由环境接管，不能从文件混入地址或密钥。轻模型共用默认模型的端点、密钥和 `reasoning_effort`，需确认两者均支持此参数；不同端点或推理参数需求留待实际需要时扩展。轻模型留空恢复默认模型，切换不重跑已经处理的资料。固定连接检查可分别验证主模型和非空轻模型，均只发送测试文字；它不验证教学、图像或真实群连接。

2026-09-09已在同一个隔离虚构家庭中，通过网页设置切换两种实际可用的模型配置，分别完成日历查询、教学草稿、独立进程整理通知及核对后生成待办。两种模型实际请求均成功，原档案与日历保留，重启不重复调用。此验证覆盖接口和状态兼容；部分教学步骤仍偏通用，教学质量尚需改善，不能把格式通过当作效果合格。试用家庭的生产模型未据此自动切换。

家长教学指南仍使用默认模型。本轮生成或调整时，仅读取直接关联到本题原始记录或本题各次尝试的同孩“补充观察”，排除儿童短引导尝试来源；按记录日期、编号取最新6条并提示更早数量，观察附件保留但不读取。这些观察不进入儿童提示请求。本次观察依据变化会使未确认旧草稿失效，允许立即重新生成并在确认时复核，已确认指南保留。隔离后端、LLM传输与边界及360／1440浏览器检查已通过；同一豆包模型的两个虚构情境已核对反馈调整与暂停。上述检查不代表广泛教学质量或教育效果已经验证。

模型保存后可点“检查已保存的连接”，仅发送固定测试文字并验证响应及要求的返回格式，不发送家庭学习记录；图片和语音能力仍须单独验证。家庭设置已通过隔离 API、360／1440 CSS 像素浏览器及空家庭源码包检查，实际手机和全新 Mac 整机尚待实测。

这些设置保存在本家庭私有资料中。消息仍由下面的采集电脑、已登录 CLI 和系统定时器读取；配置完成不表示已经读到消息。应用不会通过网页安装微信／QQ、复制登录态或运行用户填写的任意命令。CLI路径及系统服务仍由部署者配置，浏览器只展示实际运行结果和缺口。页面配置和管理员文件配置共用同一授权内容；不要同时编辑私有文件和保存页面。

## 错题照片批量核对（网页与命令行）

r80新增试卷错题的拍照核对，网页入口在“更多 → 试卷错题集”。流程：选孩子、日期和可选科目 → 拍照或选已上传照片（每次1–3张，自动勾选新上传照片）→ 点“框出疑似错题”才调用模型（打开页面不调用、不预计算）→ 原图上叠框显示错题整题（红）、手写区域（蓝）、版面块（绿），下面逐题卡片预填题号、题面、学生原答、可见订正，模型不确定处单列 → 家长改文字、勾选保留/放弃、写备注 → 保存后每条勾选错题成为一条“学习进展”记录（来源固定“错题照片核对”），关联该照片原件，可在“学习任务与进展”继续记订正和复测。

边界：框选与转写都可能错，保存的只是家长核对过的错题；不判知识点、错因、掌握程度，不自动关联学习目标或生成加练任务；草稿只在当前页面，保存失败保留本页内容并可用原请求重试（同一批重复提交幂等，不产生重复记录）；未配置模型时按钮报错但照片上传和手动记录不受影响。每批最多3张、合计20MiB，标注接口等待上限150秒、HTTP连接180秒；真实`doubao-seed-evolving`合成两页约25–33秒。

后端接口（家长Token）：`POST /api/wrong/annotate`（入参`child`、`subject_hint`、`attachments`，逐张校验图片类型和大小后请求级调模型）与`POST /api/wrong/save`（入参孩子、日期、科目、`request_key`、`items`，逐题校验并调既有记录保存，幂等键为`批次-序号`）。新增`family_wrong_review.py`与`wrong-review.js`/`wrong-review.css`，标注核心仍是`family_wrong_questions.py`（命令行，用法见下）。

命令行（供批量或外部验证）：

```bash
python3 family_wrong_questions.py [--subject 数学] [--out 输出目录] [--data 私有数据目录] [--timeout 90] 照片1.jpg 照片2.png
```

- 仅接受 JPEG、PNG、WebP（按文件头识别而非扩展名），单张不超过20MiB，每批自动按3张分批，批内合计不超过20MiB。
- 输出 `wrong-questions-时间戳.json`：每页错题整题框、手写区域、版面块的坐标（各自边长归一化0..1000，原点左上），以及题号、题面转写、学生原答案、可见订正和待核对说明；安装 Pillow 时每页另出 `wrong-preview-pageN.jpg` 画框预览（仅预览需要Pillow，核心功能不需要）。
- 沿用同一份家庭模型配置（网页配置的私有 `model.json` 或 `FAMILY_LLM_*` 环境变量），使用默认模型而非轻模型；调用计入模型用量台账，模型未配置或失败时退出码为2并保留原件，手动录入不受影响。
- 结果只供核对：坐标和文字均可能有误，程序已做页号/范围/去重/阅读顺序的确定性校验，但不判知识点、错因或掌握程度；命令行不写家庭数据库。

验证：真实豆包 `doubao-seed-evolving` 对程序生成的虚构数学/语文小测（错题框精确率/召回率均1.00，错误答案与订正全部转写正确，含错字“窗处”→“窗外”）、真实HTTP全链路、360/1440浏览器流程均通过；真实作业、儿童手写和手机拍照质量仍待家庭环境验收。

跨模型验证：OpenRouter 已确认提供零费用的 `inclusionai/ling-3.0-flash-vl:free`（另有同模型付费档）。要试验时在网页“家庭设置 → 模型连接”或私有 `model.json` 配置：基础地址 `https://openrouter.ai/api/v1`（Chat Completions），模型名 `inclusionai/ling-3.0-flash-vl:free`，密钥填 OpenRouter API key；先只对虚构或可公开材料验证坐标质量，不要直接切换家庭生产模型。不同模型的坐标系遵守程度不同，切换后必须重跑合成案例核对框选质量，质量未验证前不作为错题录入依据。

## 新 Mac 同机运行

应用、Python 和所需第三方工具已安装后，在普通用户的图形登录会话中配置，不使用 sudo：

若刚才在终端手动运行了本应用的 `python3 app.py`，先在原终端按 Ctrl+C 停止它，再安装登录服务，避免端口占用。也可以直接安装服务，无需先手动启动；已有家庭服务按升级流程维护，不重复安装。

```sh
python3 configure_mac.py
python3 configure_mac.py --install
```

第一条只显示计划，无写入或启动；第二条明确安装加载本应用服务。默认程序目录为脚本所在目录，可用 `--root` 指定；程序和资料在同一应用目录，`FAMILY_DATA` 固定为该目录的 `private`。默认本地网页 `http://127.0.0.1:8765`，可用 `--app-url` 改为另一本机端口；本机配置命令不配置公网或远程服务。首次安装就需要手机访问时，改用下一节带 `--mobile-url` 的安装命令，不先重复安装一次。

安装产物是 `private/collector.json` 及 `~/Library/LaunchAgents/local.family-learning.web.plist`、`local.family-learning.agent.plist`、`local.family-learning.collector.plist`、`local.family-learning.backup.plist`。网页服务保持运行，Agent 每分钟检查，已配置的采集器每轮结束后等 5 分钟查询到期来源；无 CLI 时采集器保留停用。各进程使用同一应用目录，日志位于私有目录。首次打开网页创建孩子、绑定来源后，再明确启用 Agent；进程已加载不表示消息或模型已连通。

首次安装会先启动网页，最多等待10秒，通过本机状态接口完成空数据库初始化，再加载 Agent、已配置的采集器和备份服务。网页或数据库未就绪时撤销本次安装的服务与配置，保留已有资料，不将仅注册服务视为安装成功。

备份登录服务在加载时及每天本地时间02:00执行 `family_backup.py --root 应用目录 daily`，每天只保留一份本地日期归档，已有包需校验有效才跳过；失败保留已有备份，通过私有日志报告，不自动停服务、恢复主库或删除旧包。后续运行若数据库丢失仍明确失败，不当作空家庭跳过。休眠后的系统补跑仍须查看实际回执；不把加载或计划时刻当成已备份。

已有安装仅增加备份服务时，先更新同版程序，用 `--output-dir` 输出到一个新的私有检查目录；只核对并安装其中 `local.family-learning.backup.plist`，保留已有网页、Agent、采集、打印与访问配置。不要重跑完整 `--install`，不要把生成的其它配置覆盖当前家庭。该任务不读取微信/QQ数据库、不发送资料到外部，也不能替代另存异机备份。

`--wechat-cli /absolute/path/to/wechat-cli` 可指定已安装可执行文件，省略时从 PATH 探测；`--qq-cli /absolute/path/to/qq_reader.py` 可指定兼容只读脚本。仅生成检查文件可用 `--output-dir /absolute/path/to/new-directory`，不会安装或启动服务。未发现 CLI 可先使用网页；后续补接需按下文核对采集配置和登录服务。

已有服务、私有采集配置或占用端口会使安装停止，保留原部署；不自动更新或卸载既有服务。升级沿用备份和服务维护流程。Mac 休眠、关机或退出登录会中断运行，登录服务不等同于无人登录的服务器服务；实际冷开机恢复与手机访问需另验收。

`python3 test_configure_mac.py` 已通过隔离测试，覆盖配置产物、私有权限、已有安装保护和失败撤销；未在生产机器重复安装，也未将模拟启动结果当作新 Mac 的实际登录与消息验证。

2026-09-10 已在另一台 Apple Silicon Mac、macOS 26.5.2 / Python 3.12.14 上，用虚构家庭实际安装系统服务，核对 Agent 与备份执行成功、网页进程自动恢复及记录保留；测试服务已撤除。首次备份早于数据库创建的问题已修复，冷开机、手机、真实来源及打印仍单独验收。

## 公网入口、本机运行

手机 → 自有服务器的 HTTPS `/family/` → 加密反向连接 → 家庭电脑 `127.0.0.1:8765`。网页、Agent、唯一数据库、消息工具和打印仍在家庭电脑，服务器只负责入口和转发，不另起业务后端。现有家庭恢复公网时复用原配置，不重复安装服务或复制开发数据库。

1. 备份正在使用的主库、原访问配置、网页服务文件及旧程序；更新已验证的公开程序文件，保留原私有资料、模型和消息配置。
2. 将原 `private/access.json` 的 `base_url` 改为 `https://家庭域名/family`，保留原账号、盐和密码哈希。网页服务设置 `FAMILY_CHILD_PUBLIC_URL=https://家庭域名/family/child/`、`FAMILY_CHILD_COOKIE_PATH=/family/child/`、`FAMILY_CHILD_SECURE=1`。受控重载网页后，公网配置仅监听本机回环地址。
3. 复用已有反向 SSH 连接，把服务器回环端口映射到本机网页端口；服务器转发端口不直接对外开放。Nginx 将 `/family/` 转发到回环端口并去掉该前缀，覆盖 `Host` 为 `localhost`、`X-Forwarded-Proto` 为实际 `$scheme`；不得删掉协议头，否则请求可能误入本机工具入口。保留 Cookie/Set-Cookie 及家长 Authorization；`/family/child/` 单独转到 `/child/`，清空 Authorization，继续由儿童接口校验会话。
4. 从实际公网入口核对：未登录 API 返回401、普通网页登录、半年 Secure 会话、今天/日历、保存重开、原件与订阅各自认证，手机/桌面布局及退出。代理保留原件大小限制、合理读取超时和 `Cache-Control: no-store`。改动服务器配置时先备份、检查语法，再仅重载受影响配置。

失败恢复代码和入口配置，不能用旧数据库覆盖家庭新记录。局域网配置与公网配置是同一实例的两种入口选择；当前配置的地址为家长入口，不同时开放未认证的局域网工具端口。公网HTTPS无需手机安装家庭证书；真实iPhone及微信内使用仍需实测。家庭电脑需开机、登录并联网，休眠/断网或隧道断开期间公网也不可用。

## 家庭实例升级与回退

**无损升级包含登录状态。** 常规更新/代码回退保留原主库中的家长和儿童会话、账号配置、Cookie路径和到期时间；不得用隔离恢复库替换正式主库。隔离灾难恢复清会话的规则仍保留，不用于普通升级。发布前后检查原有未过期会话逐条保留，不能把整个会话表列为可随意变化的例外。用升级前已登录的浏览器跨进程重启后刷新、读取和保存核验；再次输入密码登录成功不能替代这项验收。生产验收新建的会话在finally中调用退出，仅清理自己的Cookie，不挤掉、批量清理或延长家长会话。

这是已有家庭从当前运行版本切换到已验收main的执行门槛；源码合入不自动触发部署。先冻结候选提交与当前运行提交，在私有交付记录中写明唯一应用路径、SQLite路径、附件路径和全部写入进程（网页、Agent、采集、计时/打印桥接及维护工具）。保留原公网入口、来源游标与家长配置，不新建另一套家庭后台。

1. **备份与演练。** 用当前运行代码的 `family_backup.py --root 应用目录 create private/backups/升级前.zip` 生成完整备份，访问/模型配置、服务文件、消息工具配置和旧代码另存私有副本；这些配置不全在数据归档内。用候选版本的 `family_backup.py restore 备份文件绝对路径 全新空目录` 恢复（目标父目录先存在，目标须不存在或为空），检查归档校验和、SQLite完整性、业务数量与关联、原件、日历UID及来源游标。恢复工具会停用Agent、清除会话/邀请、暂缓打印及计时，故恢复后数据库字节不应要求等同原包；原包哈希须先通过。配置文件与核验材料不公开。
2. **隔离验收。** 在恢复副本放置同一候选代码，仅用单独回环端口和隔离的访问配置打开网页；确认所有外部模型、采集、打印和执行进程不启动，不注册第二套服务或公网转发。核验登录、今日作业/学校事务、日历、原件、学习目标、画像来源和更正的保存重开；合成资料检查不能替代恢复副本的结构兼容性检查。副本的测试写入绝不回灌家庭主库。
3. **切换唯一写入点。** 约定短暂停写窗口，停下已核对的全部写入者并确认无遗留手动进程，做停写后的最终一致备份。只更新原应用路径中的公开代码，保留原主库、附件和私有配置；不运行首次安装 `--install`。先启动原网页服务，从既有公网入口核对匿名401、家长登录、半年会话、页面与原件、保存后重开、权限和版本；随后按原配置逐个恢复Agent、采集与桥接，确认它们全指向同一主库、来源游标未重置且无重复处理。保留恢复时间与验收证据。
4. **失败回退。** 立即停受影响写入并保存故障时的主库、附件和日志。通常只退回旧代码及必要入口配置，保留切换后新记录；先确认旧代码兼容当前数据库。若不兼容，不直接用旧备份覆盖主库：在隔离副本核对新增/更正数据及迁移可逆性后制定恢复方案，核对无数据丢失才恢复入口。隔离演练未通过、写入者未清点或无法保护新记录时不切换。

r70源码复核时未执行切换；r71已对当时验收main完成两次备份/隔离恢复核验并更新家庭实例，原主库和公网入口保留。r74再次按本节门槛完成升级前/停写后备份、42表隔离恢复、公开文件一致性、来源游标、本机接口和认证公网入口核对，并恢复原网页、Agent、采集、打印及隧道服务。r75学校消息整理恢复先在真实积压批次核验再发布；r76考试事项结果精确回流在349项Python及两套360/1440浏览器流程通过后，完成升级前备份、42表隔离恢复、140个公开文件一致性、主库完整性及本机/认证公网入口核对。手机/桌面宽度的软件检查与实际家长入口分别留证。后续增强不阻止已通过当前门槛的独立批次上线；真实手机、微信内访问、打印出纸和持续教学效果仍需分别验收。现有微信采集宿主需要保留进程时，可在短暂停写窗口暂停并恢复原进程及其子进程，不借升级重启客户端或触发重新授权。

## 局域网和公网双入口

保留原 `private/access.json` 的公网HTTPS地址和账号密码，在原网页服务环境中增加 `FAMILY_LAN_URL=http://192.168.50.2:8765`（替换为家庭电脑实际内网IP，端口必须等于原应用端口），重载同一个网页服务即可。不要重新安装、复制数据库或停止原公网隧道。应用增加局域网监听，仍只有一个网页进程和唯一主库。

两个入口都要求家长登录，使用同一账号、分别保存180天会话；局域网根路径的Cookie不带Secure，公网仍保留原HTTPS的Secure Cookie与子路径。局域网只接受已配置Host及端口、内网直连，拒绝转发头和不匹配的登录Origin；一个入口登录不会注销另一个入口。关闭双入口只需移除该环境变量并重载，原公网入口保持。

此附加入口为家长HTTP访问；网页麦克风、日历HTTPS订阅和已生成的孩子邀请继续使用公网HTTPS。手机须连同一家庭Wi-Fi，路由器可为家庭电脑保留固定地址；Mac需保持开机、联网和唤醒。局域网HTTPS的单入口方案仍见下节，附加HTTP入口不要求手机安装证书。

## 仅本机局域网访问

家庭电脑运行网页、Agent、数据库、消息采集与打印；手机连接同一家庭网络直接访问，无需Joy、公网域名或外部反向代理。外部模型仍按家庭已有配置调用，局域网入口不表示模型改为本地或已离线运行。

新安装示例：`python3 configure_mac.py --install --mobile-url http://192.168.50.2:8765`。替换为本机实际RFC1918内网IP，也可使用实际可解析的`.local`名称；HTTP入口不能带子路径，端口必须与`--app-url`一致。未配置手机入口及HTTPS部署仍只监听回环；显式内网HTTP配置才监听局域网。家长仍须登录，Cookie为HttpOnly、SameSite=Lax、180天；仅HTTPS使用Secure。非法来源、未知Host、代理转发和错误登录Origin继续拒绝，不能用伪造localhost跳过远端认证。

已有家庭只修改原本地`private/access.json`的`base_url`，保留账号、盐和密码哈希；将原网页服务的`FAMILY_CHILD_PUBLIC_URL`、`FAMILY_CHILD_COOKIE_PATH`改为对应`/child/`入口、`FAMILY_CHILD_SECURE`设为`0`，然后受控重载网页服务。先备份这些配置和服务文件，核对实际局域网登录与记录后再停止本应用旧外部转发，失败恢复配置和代码，不能用旧库覆盖家庭记录。原Agent、采集器和打印桥仍通过127.0.0.1访问，无需改动。

浏览器登录、重新登录保留填写、显式重试、退出和保存后重开已用虚构家庭核验；HTTP缺少randomUUID时使用浏览器getRandomValues生成保存编号，避免设置、作业与反馈失效。真实iPhone仍须在家中Wi-Fi实际使用核对。Mac休眠、关机或断网时手机无法访问；IP由路由器改变后需同步访问地址，可在路由器给家庭电脑保留固定地址。HTTP页面不能直接使用浏览器麦克风和手机日历订阅，可用键盘语音输入和文件上传；需要这两项时按下一节改用家庭证书的局域网HTTPS。

### 局域网 HTTPS：家庭证书（main 增量，真实 iPhone 待验收）

浏览器录音和 iPhone 日历订阅要求 HTTPS。本应用可以自己在家庭电脑上提供 HTTPS：用系统自带的 `openssl` 生成一份只属于这个家庭的私有 CA 和一张包含家庭电脑内网 IP（可加 `.local` 名称）的服务器证书，网页进程在第二个端口直接终止 TLS。不申请公网证书、不上传任何内容、不依赖 Tailscale 或 Joy；Agent、采集器和打印桥仍走本机 `http://127.0.0.1:端口`。证书目录 `private/tls/` 为 `0700`，四个文件 `0600`，不进入家庭数据备份，换机后重新签发。

新安装：`python3 configure_mac.py --install --mobile-url https://192.168.50.2:8443`。IP 换成本机实际内网地址；HTTPS 端口须与 `--app-url` 的 HTTP 端口不同。安装器生成 `private/access.json`、`private/手机访问凭据.json` 与 `private/tls/`，网页服务同时监听 `127.0.0.1:8765`（HTTP）和 `0.0.0.0:8443`（HTTPS）。已有 `private/tls` 或访问配置时拒绝安装；安装失败会撤销本次证书。

已有家庭（不重装）：

```sh
python3 family_tls.py --data private issue --host 192.168.50.2
python3 family_tls.py --data private status
```

然后把 `private/access.json` 的 `base_url` 改为 `https://192.168.50.2:8443`（保留账号、盐和密码哈希；改地址会让家长旧会话失效，需重新登录），网页服务的 `FAMILY_CHILD_PUBLIC_URL` 改为 `https://192.168.50.2:8443/child/`、`FAMILY_CHILD_SECURE` 设为 `1`，再受控重载网页服务。先备份这些配置，核对 `private/web.stdout.log` 出现“家庭局域网 HTTPS”并在手机实际登录后，才停用原 HTTP 局域网入口；失败恢复配置和代码，不动数据库。

手机首次使用：把 `private/tls/ca.crt` 隔空投送到 iPhone，或在手机 Safari 打开 `https://192.168.50.2:8443/family-ca.crt`（该地址无需登录，只返回公开的 CA 证书）。iPhone 依次进入“设置 → 已下载描述文件 → 安装”，再到“通用 → 关于本机 → 证书信任设置”为该证书打开完全信任；Mac 上可双击导入钥匙串并设为始终信任。之后打开家庭地址登录，录音、日历“同步到手机”的 HTTPS 订阅地址和孩子入口都在同一证书下。安装的是家庭自己的根证书，只应装在家里的设备上；不要把 `ca.key` 或 `server.key` 发给任何人。

续签与换 IP：服务器证书有效期 820 天（Apple 对用户安装的根证书签发的服务器证书限制为 825 天内）；到期或路由器分配了新 IP 时，再次运行 `issue --host 新IP` 会沿用原 CA 重新签发，手机无需再装证书；同时把 `access.json` 的地址改为新 IP 并重载网页。删除 `ca.key`/`ca.crt` 后重新签发会生成新的 CA，所有手机都要重装。启动时证书不含 `base_url` 的主机名只提示，不阻止运行；缺少证书或 HTTPS 端口与 HTTP 端口相同则网页拒绝启动，日志说明原因。

本轮验收：`python3 test_tls.py` 覆盖签发/续签/权限/只读 CA、Python 客户端按主机名验证、应用 TLS 监听的登录、Secure Cookie、日历 Basic 认证、转发头与未知 Host 拒绝、明文或停滞连接不阻塞其他连接、启动拒绝；`node test_lan_https_ui.cjs` 在 390/1440 像素通过家庭证书完成登录页证书提示、浏览器实际录音上传、日历订阅地址、重开后原件仍在与退出。另用公开源码在本机隔离目录以真实内网 IP 完成 `--install`，网页进程被终止后由系统恢复且 HTTPS 仍在，备份包不含证书，测试服务已撤除。真实 iPhone 安装证书、录音和日历订阅仍待家长在家中验证。

## 单机手机 HTTPS 入口

已有反向代理若把 Host 改为 localhost，升级本版本前须先准备 access.json，并让代理保留家长 Authorization 头；带转发头的请求不再沿用本地 CLI 的免登录入口。可用 `--output-dir` 生成待核对文件，但不要覆盖现有服务、采集配置或主库。原有 FAMILY_HOST/FAMILY_USER 身份头模式只在没有 access.json 时保留；配置口令后家长改用口令登录，不自动叠加另一种身份权限。


已配置访问口令时，浏览器打开 HTTPS 根入口会显示普通家长登录页，登录保存 180 天（约半年）；“更多 → 退出这台设备”只撤销当前浏览器会话。登录失效时保留当前页输入，在当前页重新登录并核对后，再自行重试保存；不要刷新未保存的表单。现有 Basic 认证继续支持 API 和手机日历，儿童仍只使用自己的邀请与会话。

已有代理若额外设置了 Basic 弹窗，须在应用升级并核对匿名 API 为 401 后，将家庭路由的认证交给应用，移除该路由额外的 Basic 层，不能关闭应用认证。保留 Authorization、Cookie、Set-Cookie 及 HTTPS 转发头；若挂在 `/family/`，访问配置须包含相同路径，登录页与会话 Cookie 才能匹配。儿童路由继续隔离家长 Authorization。先验证普通登录、匿名拒绝、退出与日历认证，再邀请家庭试用。该网页是 H5，微信小程序和微信对话入口尚未发布。

家长会话只保存令牌哈希、到期时间和访问配置指纹；修改访问配置使旧会话失效，备份恢复会清空家长和儿童会话、儿童邀请，恢复后重新登录。当前为同一个家庭账号的多浏览器会话，不是完整的多家长账号管理。

以下用于下一家的首次安装：Mac 与获准访问的手机已安装、登录 Tailscale，并位于允许相互访问的家庭 tailnet。Serve 只在 tailnet 内提供服务，仍受其访问规则限制；HTTPS 未启用时，命令会提示打开管理授权页面，可能需要管理员登录。不要启用 Funnel 或运行 `tailscale funnel`。本节使用独立设备域名的根路径，不需要增加 `/family` 挂载。[官方 Serve 前提](https://tailscale.com/docs/features/tailscale-serve)

1. 将示例域名 `family-hub.example-tailnet.ts.net` 换成这台 Mac 在 Tailscale 中显示的 DNS 全名。先查看计划，再在尚未安装本应用服务的新 Mac 上安装：

   ```sh
   python3 configure_mac.py --mobile-url https://family-hub.example-tailnet.ts.net
   python3 configure_mac.py --install --mobile-url https://family-hub.example-tailnet.ts.net
   ```

   `--mobile-url` 只生成应用配置，不安装 Tailscale、不申请证书，也不代表 HTTPS 已接通。安装器生成 `private/access.json`（家长账号、盐及口令哈希）和 `private/手机访问凭据.json`（家长账号及随机明文口令），两者均为 `0600`；只在本机私下查看凭据，不贴到日志或公开仓库。仅网页 LaunchAgent 增加孩子 HTTPS、安全 Cookie 路径和稳定日历标识配置；Agent、采集器继续访问原本机回环地址，不携带家长口令。

2. 先运行 `tailscale serve status` 核对是否已有转发；已有配置时先由维护者核对，不覆盖其他服务。确认可使用这台设备的 HTTPS 443 根入口后，将其转发到本应用的回环端口：

   ```sh
   tailscale serve --bg --https=443 http://127.0.0.1:8765
   tailscale serve status
   ```

   若安装时改了 `--app-url` 端口，这里的目标端口必须一致。核对 Serve 输出的 HTTPS 域名与安装参数相同，目标为 `127.0.0.1`；应用仍只监听回环，不开放局域网或公网 HTTP 端口。`--bg` 是官方后台运行选项，HTTPS 由 Serve 终止并提供证书；这些是配置行为，不代替本项目的重启验收。[官方命令与参数](https://tailscale.com/docs/reference/tailscale-cli/serve)

3. 手机连接获准的 tailnet 后打开该 HTTPS 地址，使用私有凭据文件中的家长账号和口令登录。家长口令保护网页、家长 API、原件及日历订阅；孩子仅使用家长在网页生成的一次性邀请，邀请仍放在链接的 `#invite=` 片段，不向孩子提供家长口令或已登录家长的浏览器。先核对登录、记录读取与孩子入口，再分别实测拍照／录音、日历首次认证与后续刷新。真实手机、服务重启、冷开机及全新 Mac 整机尚未验收。

已有家庭升级应先备份，按原服务维护流程更新代码与私有配置；不要重复执行 `--install`，也不要复制新安装样例覆盖主库。上述两份访问配置不包含在家庭数据备份中，恢复后须私下重新配置并核对权限，再开放入口。当前试用家庭仍保留既有远端认证 HTTPS 入口；本流程不表示已完成替换或可以停用原入口。

## 先核对本机路径

以下 Linux 模板假定程序在 `~/family-learning`，Python 3.10 或以上位于 `/usr/bin/python3`；路径不同时先改模板。正式部署先按 README 初始化家庭、启用应用和认证入口，并保留一致的主库及附件备份。网页模型配置保存于私有 `private/model.json`；原有环境管理方式继续使用 `private/model.env`且优先，不把密钥写入模板、命令行或公开仓库。两个模型配置文件均不进入家庭数据备份，换机器或恢复后单独重配。

网页配置来源后无需再创建另一份授权文件。管理员也可在首次启动前创建 `private/agent.json`，先只处理网页中已有记录，不接消息来源；已有文件不可直接用下面的空示例覆盖：

```json
{"enabled": true, "sources": []}
```

```sh
chmod 600 private/agent.json
```

缺少配置时 Agent 默认停用。`sources` 最多 20 项，每项必须包含以下字段，不能增加其他字段：

| 字段 | 内容 |
| --- | --- |
| `id` | 已授权会话的真实标识；微信为 chatroom ID，QQ 按已核验 CLI 的群标识填写 |
| `platform` | `wechat` 或 `qq` |
| `child_id` | 本家庭已存在的稳定孩子编号，如 `child-1` |
| `name` | 家长能够辨认的来源名称 |
| `cursor` | 字符串；已有来源保持已确认读取位置；网页新增微信来源使用 `0` 作为本机记录起读位置，不表示已经读取 |
| `enabled` | 布尔值；核对身份与范围后再设为 `true` |

新微信来源从本机可读取记录起点分批处理；`0`只是起读请求，不表示完整历史已覆盖。只有后台确认接收才推进游标，旧来源不重置。QQ没有可核验原生历史时保留未就绪状态。不凭群名猜孩子，未配置的来源不扫描，历史和附件缺口单独保留。首次接收之后以主库保存的游标为准，修改配置中的初始游标不会重置它；已有来源不能改绑到另一孩子。

## 应用主机的定时执行

确认私有 Agent 配置后，先在程序目录运行一轮：

```sh
python3 family_agent.py --once
```

该命令结束后退出，定时唤起由 systemd 完成。直接从终端运行时，模型变量须已导出到当前进程；创建 `model.env` 文件不会自动注入终端环境。下面的 service 会读取此文件。检查真实退出状态及网页中的实际结果；仅进程存在或定时器已启用不代表模型、消息或回看成功。

安装同一普通用户的 systemd 模板：

```sh
mkdir -p ~/.config/systemd/user
cp deploy/family-agent.service deploy/family-agent.timer ~/.config/systemd/user/
systemctl --user daemon-reload
systemctl --user enable --now family-agent.timer
systemctl --user list-timers family-agent.timer
journalctl --user -u family-agent.service -n 30 --no-pager
```

模板每分钟唤起一次，不以家长访问页面作为触发条件。同一个 service 仍在执行时 systemd 不会并行启动第二次；恢复后会补一次错过的定时检查。主机需在用户退出后继续运行时，由该机器管理员启用该用户的 lingering，例如 `loginctl enable-linger "$USER"`。模型服务仍须独立保持可用；若模型通过另一台电脑的隧道提供，那台电脑离线时模型能力暂不可用。

手动停启调度：

```sh
systemctl --user stop family-agent.timer
systemctl --user start family-agent.timer
```

停止 timer 不会中断已开始的一轮。迁移或恢复主库前还须等该轮结束，并停止网页写入和采集。

## 消息电脑的采集

只为已授权、实际可读的来源配置采集。创建 `private/collector.json` 并设为 `0600`：

```json
{
  "app_url": "http://127.0.0.1:8765/",
  "wechat_cli": "/absolute/path/to/wechat-cli"
}
```

可选 `qq_cli` 填已核验的 QQ 只读脚本绝对路径。配置只接受上述字段：不在这里填写群号、孩子、游标或密码；会话及游标从后台授权列表获取。`app_url` 仅接受 HTTP 回环地址，不能包含账号、查询串或子路径；远程后台通过已经建立的 SSH 回环隧道访问。不要把应用无认证暴露到公网来连接采集器。

```sh
chmod 600 private/collector.json
python3 family_collect.py --config private/collector.json --once
```

每轮每个微信来源最多取一页 200 条，后续轮次从后台确认的游标继续；非文本消息保留未读内容缺口。QQ 通过配置的只读 CLI 每页最多读取20条，向前最多10页找回上次已保存的消息，再按群内消息序号排序、去重并提交最早的连续未读部分；消息ID只作身份，不推定其数值越大就越新。找不到锚点、分页停滞、同消息原文冲突或读取失败均保留原游标。读取共享25秒预算，另留5秒给回执I/O；这是超时与截止检查，不是操作系统硬实时保证。采集器不自动登录或重启QQ，图片、附件及首次范围以前的历史仍单独标明缺口。

首次接入须核对CLI与指定群的授权配置，然后明确执行一次 `python3 family_collect.py --config private/collector.json --bootstrap-qq`。会话已捕获但尚未验证读取时，允许进行有界原生读取尝试；明确离线时不读取，状态接口响应不计为采集成功。该操作只处理已授权且游标为空的QQ来源，把实际首个原生页和对应游标一起提交；空页不建立游标，也不触及微信或已有QQ游标。结果中的 `earlier_history_verified: false` 表示此前历史尚未覆盖；不能直接填一个最新ID来跳过未读记录。后续使用普通采集模式，只有后台确认接收才推进。更换采集程序须受控重新加载运行进程，旧Python进程不会自动加载新增分页逻辑；仅更改CLI路径不能让旧解析器接受多页结果。跨页方向与真实QQ版本兼容性需在部署机实际核验，合成测试不能代替接入验收。

### Mac QQ本地数据库CLI（实验版）

`family_qq_local.py` 是参考qqcli-rs流程的独立Mac工具，不是其官方Mac发行包。只支持明确配置的群，当前字段候选为 `group_msg_table` 数字列；不自动搜索账号或扫描全部聊天。标准Python负责配置、查询和JSON，SQLCipher及NTQQ文件头扩展仅在一次性子进程加载，不影响Family Agent的SQLite。无截图、QQ登录、消息发送或模型调用。

需要Python 3.10+、与本机架构一致的SQLCipher动态库、ntdb_unwrap扩展，以及与目标数据库匹配的密钥。开发机已从THIRD_PARTY.md所列固定版本构建并验证Mac ARM组件；本项目不附带第三方二进制，Intel Mac尚未实测。构建SQLCipher 4.19.0时使用其官方configure/make，参数为 `--with-tempstore=yes --disable-readline`，CFLAGS为 `-O2 -DSQLITE_HAS_CODEC -DSQLCIPHER_CRYPTO_CC -DSQLITE_EXTRA_INIT=sqlcipher_extra_init -DSQLITE_EXTRA_SHUTDOWN=sqlcipher_extra_shutdown`，LDFLAGS为 `-framework Security -framework CoreFoundation`；产物为构建目录的 `libsqlite3.dylib`。在固定ntdb_unwrap源码目录运行 `RUSTFLAGS="-C panic=abort -C link-arg=-lSystem" cargo build --release -p sqlite_ext_ntqq_db --features _cdylib`，产物为 `target/release/libsqlite_ext_ntqq_db.dylib`。构建需要Xcode命令行工具、Rust及SQLCipher构建依赖；不是已经完成的新系统自动安装器。

将已有密钥在本机写入私有文件，权限设为600，不粘贴到命令参数、日志或公开配置。QQ取钥是另外的运行期步骤；当前官方签名不允许普通调试附加，CLI不会擅自改签名、重启或重新登录QQ。以下仅使用虚构路径和群号，替换为自己明确授权的值：

```sh
python3 family_qq_local.py --config private/qq-local.json init \
  --database /absolute/path/nt_msg.db \
  --sqlcipher /absolute/path/libsqlite3.dylib \
  --vfs /absolute/path/libsqlite_ext_ntqq_db.dylib \
  --key-file /absolute/private/qq-database.key --group 10002
python3 family_qq_local.py --config private/qq-local.json doctor
python3 family_qq_local.py --config private/qq-local.json sessions
python3 family_qq_local.py --config private/qq-local.json history 10002 --since 2026-09-01 --limit 50
python3 family_qq_local.py --config private/qq-local.json search 10002 听写 --since 2026-09-01
```

配置与密钥都须为本用户所有、600权限、非符号链接的普通文件；`init`拒绝覆盖已有配置。可重复传入 `--group` 明确允许的群，须与家庭网页来源一致。所有操作默认输出JSON，`--json`可放在命令前。操作成功退出0，配置或读取失败退出3；命令语法错误由参数解析器退出2。`init`成功只表示配置已保存；`doctor`明确显示key_required、依赖缺失、读取错误或结构不支持，成功只验证结构可读，不认证真实消息。诊断不输出密钥、正文或路径。

历史按时间与原64位ID升序，使用返回的 `next_cursor` 传入 `--after` 续读，日期按北京时间解释。搜索每次扫描最多200条授权群记录；即使本页无匹配，`has_more=true`仍需续读。不建立额外搜索数据库。较晚补到本地的旧消息、原消息撤回和数据库轮转尚未支持持续追溯，不能把到达本地库末尾理解为云端全历史已读。

图片、文件、引用和未知Protobuf结构标为未读；只识别确定的文字片段，不读取消息内的路径、不下载原件或展开其他群的引用。结果固定报告 `collector_compatible=false`，**不要填入现有collector.json的qq_cli字段**；实际QQ密钥/结构/自然新增/原件及正常桌面使用验收通过后，才适配原采集器的连续性核对和入库。现有失败状态、旧游标、桌面QQ保持。

可运行独立检查：

```sh
python3 test_qq_local.py --sqlcipher /absolute/path/libsqlite3.dylib --vfs /absolute/path/libsqlite_ext_ntqq_db.dylib
```

只使用临时虚构加密记录，覆盖指定群、64位ID、分页/搜索、错误拒绝和已提交WAL；不带两项原生参数时仅检查解析器，并明确报告native_checks=not_run。主文件/WAL读取前后散列不变不表示共享内存侧文件不变、崩溃恢复或真实QQ共存通过。

### Mac QQ 实验性只读适配器（桌面共存未通过）

当前同账号试用出现桌面QQ退出及操作受影响，已撤回日常采集部署。下面仅记录实验接口配置，不是桌面共存安装建议。读取器会独立登录QQ；只读HTTP请求、不修改QQ文件不能保证官方客户端会话不受影响。在有新兼容性证据前，不应给该读取器配置自动启动或自动重新登录。已有冲突实例应先停止读取器并撤销自启、停用QQ来源；保留消息与成功游标，恢复官方QQ日常使用，再核对下一步。

`family_qq_llbot.py`对接单独安装的[LLBot v8.1.10](https://github.com/LLOneBot/LuckyLilliaBot/releases/tag/v8.1.10)原生协议模式；不注入或改写桌面QQ，只用回环HTTP GET读取一个明确授权的群。LLBot、第三方服务Auth Token和QQ登录由安装者按上游说明配置，不随本项目安装或分发。该服务令牌与WebUI密码是不同凭据；本适配器只读取WebUI密码文件，不能代替上游服务令牌。

在脚本同目录新建Git忽略的 `family_qq_llbot.json`（下面全是占位示例，须替换为本家庭配置）：

```json
{"url":"http://127.0.0.1:15703","token_file":"/absolute/path/to/llbot/data/webui_token.txt","allowed_groups":["10002"]}
```

配置与密码文件均须是当前用户拥有、权限600的普通文件，不能是符号链接。`allowed_groups`当前只支持一个群，并须与网页授权来源一致；其它群不自动读取。将 `private/collector.json` 的 `qq_cli` 指向本脚本绝对路径，再按上面的首次接入步骤操作。已有游标继续普通采集，禁止清空重建。单独检查可用 `python3 family_qq_llbot.py status`，也可用 `--config` 指定私有配置文件；`self-check`仅用虚构消息，不需要账号。

本版保留原生消息ID，以群内序号分页；适配器显式报告 `history_cursor: message_seq`，旧CLI默认仍按消息ID传递翻页锚点。已在Apple Silicon Mac实测原生跨页文本读取与选定原图通过现有上传入口关联；CLI本身只同步文字，图片、表情、文件等保留内容缺口，不宣称全部历史或自动取图。桌面同时收发与复制在当前试用中未通过；自然增量及冷开机同样未验收。应用采集器不负责重启LLBot，退出、令牌失效或账号离线会保留上次成功游标并报错；本版不提供读取服务自启安装，当前已撤回的共存方案不得因历史成功读取记录自动恢复。

微信采集使用已核验CLI的 `history --view agent --order asc --strict-read-only --include-media-paths false`，零游标首次读取不传 `after_message`，后续传后台确认的消息锚点。查询顺序与本地消息编号大小并不等价，按接口原顺序及分页锚点继续；不能先取最新一页再排序冒充完整增量。旧CLI或返回契约不一致时保留失败和原游标，不静默跳过历史。微信文字采集关闭媒体路径补充，避免CLI默认遍历账号图片缓存；只继承基础环境和显式配置路径，固定禁止取钥并关闭元数据自动刷新。

macOS 可复用 `deploy/local.family-learning.collector.plist`：用文本编辑器将 `__PYTHON3__` 替换为 `command -v python3` 得到的绝对路径，将 `__APP_ROOT__` 替换为实际程序目录。路径含 `&` 或 `<` 时须按 XML 转义；launchd 不展开 `~` 或环境变量。确认 `private` 目录存在后安装：

```sh
mkdir -p ~/Library/LaunchAgents
cp deploy/local.family-learning.collector.plist ~/Library/LaunchAgents/
plutil -lint ~/Library/LaunchAgents/local.family-learning.collector.plist
launchctl bootstrap "gui/$(id -u)" ~/Library/LaunchAgents/local.family-learning.collector.plist
launchctl print "gui/$(id -u)/local.family-learning.collector"
```

模板在登录后启动并保持同一个采集进程，每轮结束后等五分钟再检查，日志保存在私有目录。正常停止不自动重启，异常退出由系统限速重启。修改已加载的模板时先 `launchctl bootout "gui/$(id -u)/local.family-learning.collector"` 再安装加载；不要重复创建其他采集任务。Mac 退出登录、休眠或 CLI 不可用时不能持续读取消息，系统应显示实际覆盖缺口。

## QQ独立宿主与窗口片段（实验接入，持续采集未验收）

`family_qq_cua.py`默认只检查运行它的应用身份、Cua SDK 0.26.0、辅助功能/录屏权限和锁屏状态；未配置窗口读取时不弹授权、不打开QQ、不读取聊天、不调用模型、不提交消息。回执写在既有采集配置同目录的`qq-cua-preflight.json`，保持私有权限。`host_ready`仅表示本次具名宿主检查通过，不代表来源已同步；开发工具中直接执行不能代替具名应用验收。

用**已安装Cua SDK的虚拟环境Python**运行下面的构建命令。复用原`private/collector.json`只为核对安装位置，不修改配置或调用其中的微信工具。生成目录必须全新，构建不安装SDK、不启动或替换服务：

```bash
/已安装cua环境/bin/python family_collector_mac.py --root "/实际应用目录" --qq-preflight --output-dir "$HOME/Library/Application Support/Family Learning/qq-reader-host"
```

生成`FamilyQQReader.app`，显示名称“一起成长 QQ 读取”，应用标识`local.family-learning.qq-reader`。它在原生应用进程内加载现有Python动态库与SDK，保留虚拟环境路径；不会把检查交给通用Python子进程。单独打开应用只检查一次；生成的LaunchAgent没有周期和自动重试，也不随构建启用。读取器与微信原采集入口分开，不切换正在工作的微信服务。

实际检查得到`permission_required`时，由家庭在macOS“隐私与安全性”的“辅助功能”和“屏幕与系统音频录制”（名称随系统版本不同）中核对并授权该应用，再重新打开检查。无需全盘访问权限。SDK的宿主提示字段可能为空，因此同时用CoreFoundation读取实际进程应用标识；不会手写环境变量冒充系统身份。当前家庭具名宿主仍缺两项授权，QQ持续读取未恢复；下面的窗口片段路径已通过隔离软件检查，真实群与桌面共存仍待验收。

实验性单次窗口读取：仅在指定来源及孩子已在家庭设置中核对、来源已启用、具名宿主权限实际通过后，由部署者在既有采集配置同目录的`qq-cua.json`填写`{"enabled":true,"source_id":"qq:123456"}`（群号为虚构示例），文件权限0600。没有此文件或设置为false，仍仅检查。打开既有具名应用可运行一次，不需要Codex；未配置后台入口时只运行这一次。

如需由既有Agent后台周期调用，在同一`qq-cua.json`增加`host_app`，值为构建所得`FamilyQQReader.app`的绝对路径。仍须手动核对来源与具名宿主权限；本家庭当前未启用此配置。程序核对应用标识、原生可执行文件及内嵌的家庭目录/采集配置，拒绝其他家庭或任意程序路径；不接受网页传来的启动命令。

后台沿用既定30/60分钟间隔，开始前保存`qq-cua-attempt.json`，下轮依据尝试时间等待；锁屏跳过，尚无权限/读取失败也不在每个Agent轮次重试。QQ窗口与原协议分别计时，`/api/agent/fragment/plan`只提供已授权QQ来源，不受原协议重试间隔影响，仍由家长身份与原绑定校验。新回执必须匹配来源、实际应用身份和本次时间；读取进程有50秒上限，失败清理只针对该次子进程组，父进程同时清理本轮临时窗口文件，不结束官方QQ。窗口失败不会停止其他学习跟进。未配置、来源停用或缺少`host_app`时不会启动应用；不新增LaunchAgent或定时器。生产入口未启用前，虚构原生进程检查不代表实际消息或权限已验证。

读取限于已打开的官方QQ和当前选中的授权群；群名、群号、窗口和布局必须核对，正在编辑消息、锁屏或窗口不明确时停止。必要时仅通过后台输入展开/关闭群资料，当前不自动切群、翻页、打开附件或发消息。完整窗口及界面树只在本机临时目录处理；上传前按消息可见区域裁剪，排除聊天列表、成员名单和输入框，临时文件随后清理。当前布局和后台操作尚待真实机器验收，不能凭合成检查认定对全部QQ版本有效。

`/api/agent/fragment`复用家长认证和已启用来源授权，原子保存窗口片段及图片，重复相同内容不会重复建项。片段不是老师附件原件，不伪造原生消息ID、发送者或发布时间，不改原消息游标、最近成功/失败和既有历史。采集时间另存；原生消息入口拒绝窗口片段类型。来源页可直接查看片段，沿用后台图片整理及家长核对后留作学习记录；模型接收片段限制说明，不能据此自动认定成绩、完成或掌握。只保存内容一致的重复片段会去重；重叠页去重、原图、历史衔接、自然新增及真实周期独立读取仍须继续实现/验收。

`python3 test_qq_capture.py`覆盖虚构群布局、真实像素裁剪、HTTP身份/归属、重试不重复、失败回滚、保留游标与家长移除关联、模型草稿及独立入口分派；不读取实际QQ。`test_sources_ui.cjs`覆盖手机/桌面来源入口、失败重试、重开和不误报同步成功。

`python3 test_qq_cua.py`与`python3 test_collector_mac.py`覆盖拒绝/未知权限、未知锁屏、错误应用身份、SDK变化、私有回执、虚拟环境与原生进程身份；检查不用真实聊天或系统授权。

## macOS 采集权限与重复弹窗

`--interval 300`复用同一个Python采集进程，各轮重新读取私有配置、后台授权来源与游标；不新增聊天范围、不更改系统隐私授权。`--once`仍可用于单次诊断。生成的Mac采集配置与上面的模板均使用持续进程，不再每五分钟退出后重建。

[Apple说明](https://developer.apple.com/videos/play/wwdc2023/10053/)：自动弹出的其他应用数据目录访问授权，在应用退出后会重置。因此，定时重建负责采集的进程可能重复弹窗。保持进程存活用于避免这个触发条件，不绕过用户选择；进程重启、退出登录或工具升级后仍可能需要再次授权。文稿文件夹权限与应用数据目录权限应按系统实际提示分别核对，不能只移动项目目录就认定已修好。

隔离测试验证持续轮询、失败等待、正常信号退出和生成配置；实际是否仍弹窗，须对照运行PID、下一轮真实读取和系统权限日志，不能只凭进程存在认定已修复。

本轮实机核对：解锁后原微信后台在自然轮次成功读取三个来源，新入库六条消息由原Agent处理；没有重启采集或切换入口。成功轮次同时保留AllFiles拒绝日志，因此须看具体权限服务与读取结果，不能看到该日志就要求全盘授权。锁屏后的持续运行仍单独验收。

QQ界面备选另需辅助功能和录屏权限。开发工具中SDK能读到指定群，并不证明系统独立拉起的宿主可读；本轮独立宿主预检两项均未授权，同一窗口读取失败。嵌入SDK不代宿主申请权限；后续由产品命名宿主说明用途并完成授权，不能沿用Codex的权限当作交付，也不把这两项权限加给微信CLI。

### 可选：以产品名称运行 Mac 采集器

当普通 Python 后台的权限归属不清时，`family_collector_mac.py`可为既有采集器构建“一起成长消息采集”应用入口。它只启动指定应用目录的`family_collect.py`，仍使用原CLI、服务端授权群和增量接口；不另建调度器，不把网页或Codex作为运行宿主。[Apple开发者支持](https://developer.apple.com/forums/thread/118508)建议用应用包承载需要文件访问授权的后台程序；这只提供可识别的应用身份，不自动获得授权，也不能证明微信已经恢复。

使用现有Python 3.10以上及Apple命令行工具，在固定的新目录构建：

```sh
python3 family_collector_mac.py --root "/实际应用目录" --output-dir "$HOME/Library/Application Support/Family Learning/collector-host"
```

默认读取该应用的`private/collector.json`，也可用`--config`指定已有0600私有配置。输出包含`FamilyCollector.app`及同名采集LaunchAgent配置；没有复制微信账号、密钥或消息，也不启动任何程序。路径写入应用签名，构建后不要移动目录。推荐程序和自身配置使用普通应用数据目录；这减少对文稿目录的依赖，不能代替访问微信应用数据所需的系统授权。

先核对生成配置中的程序、配置和日志路径，再按上一节替换**原同名采集服务**，保留原配置以便回退，不能同时保留旧Python采集进程和新入口。构建不负责替换服务或修改系统设置；如系统要求授权，由家庭核对名称“一起成长消息采集”后处理。锁屏时等待解锁，不重试弹窗或修改隐私数据库；不能借用通用终端/Python的广泛权限掩盖应用自身未授权。实际成功时间、读取结果和覆盖缺口仍以原来源页面为准。

原生入口保持父进程存活、限制为固定采集命令、阻止本入口重复启动；Python与读取工具在单独的子进程组中运行。正常或异常退出后清理该组，停止时先给三秒退出时间，再结束未退出的读取工具；先保留组长PID再清理，避免误伤其他程序。读取工具须保持在该组内，自行脱离的守护进程不在此入口管理范围。`python3 test_collector_mac.py`实际编译及运行虚构采集器，覆盖原程序退出后的残留工具、忽略停止信号的工具、无关进程保留、退出状态和失败清理，不读取聊天、不改TCC或启动正式服务。本构建仅作本机自托管准备，使用临时签名，重建可能需要重新授权；面向其他家庭分发原生二进制还需稳定开发者签名、公证及新机验收。目前真实后台权限和自动读取仍待验证，原`configure_mac.py`安装方式保持可用。

## 学习建议的执行与回看

后台对新学习记录生成目标、具体动作、依据、预计投入和回看日；必要时只读取同孩前一条关联记录及已确认方案。家长确认后，由现有接口同事务保存待办和采纳回执；重复确认返回原待办。延期保留原日期及修改时间，按最新明确安排生成站内回看；暂不考虑或已搁置的事项不继续催。

反馈通过原记录的“补充实际进展”入口关联；用时与结果沿用“放学后”。新反馈可以触发下一项建议，原行动及完成记录不被模型覆盖。每轮每孩最多一次学习规划请求，新记录不因当天已有建议而被跳过；只更改回看安排不重复调用模型。学习规划单次最多等待90秒，每个资料指纹的三次总尝试上限保持不变。页面读取、确认、延期和到期判断均不调用模型；模型用量以设置中的实际记录为准。

错题不逐条请求学习规划：已核对错题及其订正/复测跟进由错题诊断统一理解。某孩子某科目的错题、考试、订正/复测或其更正发生变化时，Agent在原每轮3次调用额度内、学校与学习目标工作之后，每轮最多取1次重新诊断该科；其他同科目记录只作背景，不单独触发。同一证据版本最多自动尝试3次（5/10分钟退避），暂停后孩子画像显示原因，家长可点按钮重试；家长随时手动诊断不受影响。诊断与到期复测判断不在页面读取时调用模型。

## 核对与恢复

公开测试仅使用隔离的虚构资料，不读取家庭聊天或发送消息：

```sh
python3 test_review.py
python3 test_agent.py
python3 test_agent_http.py
python3 test_collect.py
python3 test_backup.py
```

实际验收须确认：无需开发会话或外部通用Agent客户端，关闭 Codex 和网页后仍能由事件或时间唤醒；一条新资料被后台接收后，Agent形成下一步、执行获准工具并保存结果，收到反馈后调整后续安排；重复执行不重复建事项；反馈、延期及更正影响下一次处理；模型或来源离线时保留旧资料并显示实际故障，恢复后继续。还须以至少两种实际可用LLM分别验证网页和后台流程，核对生效配置、权限、状态及重启恢复；当前检查不代表这些完整验收已经通过。测试数据只写隔离演示，不在家庭主库中造反馈或修改回看日期。

升级先备份，再按公开文件清单更新代码和模板，不覆盖私有配置。恢复遵循 README 的停止写入、校验备份、恢复到空目录流程。`private/agent.json` 的来源授权映射纳入数据备份；校验归档后，恢复工具仅在恢复目录将 `enabled` 设为 `false`，防止旧授权立即重启采集和模型处理。来源已确认游标、消息、建议及处理状态仍随主库完整保留，原备份不变；恢复配置的字节哈希会有意变化。旧备份不含 Agent 配置时默认停用。

另行恢复采集电脑的 `collector.json`、模型/转写配置、账号所在电脑及回环隧道；打印机设备配置已随数据备份恢复，桥接token仍需单独配置。先核对主库、原件、来源归属与当前账号，再由家庭明确启用 `agent.json`，最后恢复 timer 与采集，避免运行中覆盖数据库。损坏的 Agent 配置会阻止这次隔离恢复，不静默丢弃授权映射；修复副本并重新备份核验后再恢复。

默认不自动报名、登录、对外发消息、确认作业完成或发奖。未来增加站外提醒时，需单独配置目标和权限，并记录真实发送结果；站内显示和模型生成均不能替代送达证据。

## 可选：后台补充微信图片

不新增服务，复用每分钟的Agent进程；默认关闭，手动上传和通知核对继续独立工作。当前适配 `wechat-cli 1.6.19` 的一个已核验构建，SHA-256固定在 `family_media.py`。不同构建在核对禁止取钥、关闭媒体路径查找及资源元数据契约前会被拒绝。依赖普通Python 3.11、macOS CommonCrypto和部署者已有的FFmpeg；不使用Codex运行环境，也不打包这些外部工具。

只在微信CLI已有有效本机账号、数据库及图片密钥配置，且运行Agent的普通用户已有对应读取权限时启用。在本应用私有目录建立 `private/media.json`，替换为本机绝对路径和期望开始补图的日期：

```json
{
  "wechat_cli": "/absolute/path/wechat-cli",
  "wechat_config": "/absolute/path/wxcli/config.json",
  "wechat_config_sha256": "替换为该CLI配置文件的64位SHA-256",
  "ffmpeg": "/absolute/path/ffmpeg",
  "since": "2026-01-01T00:00:00+08:00"
}
```

该文件与既有CLI配置均须是权限600的普通文件；不在这里重复保存密钥。用 `shasum -a 256 /absolute/path/wxcli/config.json` 取得配置指纹，核对账号后填入；账号、数据库位置或密钥配置变更会停止取图，须重新核对已保存群消息与当前账号的对应关系后更新指纹。Agent每轮重新读取此配置，移走 `media.json` 即停止后续自动取图，无需重启常驻消息采集器。QQ不参与这一流程，来源授权仍来自家庭设置；恢复备份后须重新核对并启用本机连接，此配置及账号密钥不在家庭资料备份中。

每轮最多处理一条已保存、仍授权的图片；消息原文、未读状态、作业及奖励保持原值。媒体查询明确传入 `--include-local-paths false --include-debug true --strict-read-only`，只保留该条消息的资源元数据，关闭CLI默认的账号图片缓存遍历；不使用CLI返回的路径。按已核对群标识的MD5、CLI带时区时间的月份、Img目录与资源MD5，仅检查三个固定DAT文件名，不遍历目录或尝试其他群与月份。原图变体与大小必须匹配且候选唯一，拒绝符号链接；当前仅转换已验证V2/WXGF布局中的HEVC首帧，不承诺所有版本、照片、动画或文件类型。PNG显示在“原通知与原件”，文件名标明首帧。

CLI固定使用已存在的 `/usr/bin/false` 作为取钥程序，并停用元数据自动刷新；不传入其他取钥或密钥环境变量。它仍会读取本机数据库并写自己的解码缓存，不能称为零写入。CLI与FFmpeg分别限制15秒，输出、图片大小和像素均有上限。缺权限、密钥或支持格式时保留缺口，不自动申请权限或提取新密钥；同一消息至少隔5分钟重试，总计最多3次，包括中断的轮次。macOS可能向后台Python进程提出“访问其他应用数据”授权；前台查询或原常驻采集器获准不证明新启动Agent已获准。应在实际后台运行上下文核对成功，等待系统授权时保持此模块关闭，不反复启动或重置权限。用网页手动补图即可继续，不需要等自动取图恢复。

文件使用稳定编号，重启时不重复存图；关联与持久状态同一事务保存。家长手动关联优先，移除后不再自动加回，原文件仍保留。自动取到原件不表示文字已经识别、作者已确定或孩子完成了作业。已关联学校图片随后由既有Agent在原每轮3次调用额度内取最多1次，发送该消息、目标孩子和最多3张图片给家庭已配置模型，准备待家长核对草稿；不自动向孩子分享或写入学习事实。原通知页面读取缓存，不发起模型调用。源消息、孩子、关联和文件内容变化使旧草稿失效；失败沿用最多3次及单项重试，不增加调度服务。

可运行 `python3 test_wechat_media.py`、`python3 test_media.py` 和既有Agent检查验证合成数据下的转换、权限边界及重试。真实账号的首轮成功与全新Mac权限/开机验收分别记录，不能用测试通过替代。

### 老师公开资料的定期核对

老师档案与观察存于同一私有SQLite，随既有整库备份保留。“更多 → 老师档案”配置每位老师一个已确认的公开职业或教学HTTPS链接；保存与网页访问均不联网。已启用的独立Agent每轮最多检查一个到期老师，每七天一次，失败同样七天后再尝试；未配置链接不调用网络或模型。只有公开页面摘录发生变化才保存上一份成功摘录，失败保留最近成功内容，链接变更不展示旧链接内容。

读取限制为512KiB原始页面、前6000字符纯文本；不执行页面代码、不加载附件、不跟随重定向或登录，DNS结果须全为公网，连接固定已验证IP并按原主机校验TLS。十秒网络截止为协作检查，不是操作系统硬实时保证。页面正文属于待核对来源，不执行其中指令、不自动覆盖教师偏好或形成孩子的事实。调度沿用本机Agent，不新增服务；首次真正的老师链接和自然周期仍需在家庭实例另行核验。

### 登记本报功课（R15）

网页复用已配置LLM的文字/图片整理和既有语音转写服务，只发送本次所选材料。`/api/study/draft`与儿童会话对应接口仅返回草稿，不写作业。逐项核对后沿既有作业保存与幂等请求写入同一清单，原文/解释/原件引用保存在study_items的新增report列；确认孩子报告的要求与确认实际完成分开。语音服务未配置或失败时保留原音，可手填。没有新增调度、Agent或业务数据库。

2026-09-19：家庭实例按“家庭实例升级与回退”发布r82及复核修复，沿用原认证公网入口、本机唯一主库与原采集宿主。错题照片核对、后台诊断、重做草稿、间隔复测的软件已进入家庭试用；请按《待验证清单》照片核对和第10节补真实验收，不从部署日期自动推算孩子的复测日。复测日以实际已保存诊断的review_on为准，提醒须由独立Agent正常检查。


## QQ截图收集

这是本轮授权的QQ主动收集路线：双击应用目录的`QQ截图收集.command`，框选群名标题和需要整理的消息；按Esc取消。不要框进旁边的其他会话。也可在macOS快捷指令中添加“运行Shell脚本”，填写此文件的绝对路径并自行设置不冲突的快捷键。截图入口运行在家庭Mac，手机网页用于后续核对。

私有`qq-inbox.json`配置为`{"enabled": true, "source_id": "qq:123456"}`（虚构编号，替换为`agent.json`中已有的指定群；权限600），该来源和Agent均需启用。原生QQ接口对这个来源停止轮询，微信不受影响。截图保存在私有`qq-inbox/`，现有Agent每轮最多取一张；无需另设每天18点的开发任务。首次文字识别需要本机Xcode命令行工具，以Apple Vision编译一次，之后使用缓存。截图宿主需macOS屏幕录制权限；本路线无需辅助功能权限、不改QQ客户端。

文字在本机识别，整理调用家庭已配置模型；生成待核对事项、引用原图，不直接创建已确认事项。只接受包含配置群名的PNG，最大2048px、1MiB；空图、错群、识别失败保留待重试。单文件失败退避30分钟，不挡住下一张；入库成功后移到“已处理”，重复图片不重复入库。网页“来源与附件”显示待整理/失败和窗口片段；“今天”显示待核对通知，核对分类、标题、目标和日期后才保存。

**边界**：群名匹配只是误选防护，不证明发言人身份。图表、手写、折叠内容、文件卡片不等于原件已读；截图中的相对日期没有可靠发布日期时不推算。选图时Mac需要解锁；已经收集的文件可由后台整理。本轮不验证跨群自动切换、历史补采或无人值守全量同步。关闭此路线时将配置enabled改false；如不使用其他QQ路线，同时停用该QQ来源，已有原图和事项保留。

2026-09-19 r86：学校筛选及原待核对草稿刷新共用正文用途保护，未知链接不猜任务，朗读加上传保留提交关系。未新增抓取器、服务或模型供应商；仅新的筛选/既有必要刷新使用新规则，不为升级批量重跑历史消息。家庭原主库、采集游标、账号和配置保留，实际验收见待验证清单。

2026-09-20 r87：复用既有学习目标处理校验新生成建议，学校要求引用、非暂停核对方式及未知基线证据保护不新增模型调用。验证失败沿用原有有界重试与错误状态；旧待审核建议不追溯重算，已确认计划只经家长审核更新。发布继续保留唯一主库、原服务与配置。


2026-09-20 16:06 r88：原事项照片/语音反馈、已核对转写证据、方法历史及既有记录显式关联任务上线。运行提交08cc6e0，最终停写备份、42表隔离恢复和旧记录/来源游标/原件/配置保留核对通过；原网页、Agent、采集和桥接服务恢复。局域网/公网360/1440实际登录、半年会话、页面与6份原图本轮通过，公网间歇稳定性仍待持续核对。真实iPhone/微信内录音和ASR准确率另验，视频/图像理解与直接新建关联任务未交付。文档后续回填不重启家庭程序。

### 音视频上传核验（main待发布）

MP4/MOV/M4A/WebM/OGG上传及旧容器音频转写前，使用服务PATH中的`ffprobe`（已有FFmpeg组件）检查实际轨道；仅管道输入，禁止文件/网络协议，15秒截止。未安装时拒绝这些容器并明确提示，仍可保存文字/图片或上传WAV、MP3；不静默信任扩展名。视频每份最多20MB，保存回放、不转写、不理解；浏览器不支持编码时可打开原件。本机已核对网页服务PATH包含FFmpeg，部署后仍需验证实际服务，不将开发终端可用视为任意新家庭可用。


### 公网原件慢传输的分段核对

先分别测家庭回环、服务器经隧道读取、客户端公网下载，保留耗时、完整大小和哈希；本机TUN/代理可能在系统HTTP代理关闭时仍转发域名请求，需核对实际连接路线。不要把单轮公网成功当长期稳定，也不靠放宽原图超时门槛发布。只为已定位的家庭域名作必要、可回退的线路调整，不改变其他应用默认路由。手机蜂窝网络另验，桌面代理修复不能冒充手机修复。失败回退保留已兼容迁移及新数据，下次按实际表结构重新演练，不硬套旧表数量。

### 2026-09-21 r89.1 登录无损升级热修复

用户反馈每次发布后需重新登录。定位family_access登录成功时仅保留最近32个会话，发布/现场检查的新会话会淘汰未过期家长Cookie；此前只验重新登录和180天Cookie，未验升级前会话留存。

01c174a删除按数量淘汰未过期会话；到期清理、主动退出、密码/访问配置变更失效和灾难恢复撤销仍保留。合成HTTP加入35个较新会话后旧公网/局域网均有效，移除修复原断言401失败；test_access/test_child_access/test_backup通过。既有登录360/1440改为真正停止并启动服务，同Cookie、未保存表单和原请求保持，显式重试只保存一份，退出/过期/错误密码断言保留。

家庭02:45正式发布r89.1：仅将原r89的family_access.py更新为01c174a版本，未夹带main上待窗口的DOCX/照片上传候选。最终停写备份及43表隔离恢复通过；真实局域网/公网各360/1440登录、同Cookie跨真实重启、刷新均通过。验收新建会话逐一退出后，正式43表逐表内容与切换前完全一致（包含全部原家长/儿童会话），私有配置不变，原采集及服务恢复。证据private/validation/login-preserve-*与私有family-login-hotfix.json。current-release记录r89基线+单文件hotfix提交/哈希，不能宣称当前完整main已部署。已被旧规则删除的会话不私自恢复；真实iPhone由家长继续核验。
