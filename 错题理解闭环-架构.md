# 错题理解闭环 · Agent 架构（教学质量为唯一牵引）

目标不是"采集+记住错题"，而是**理解孩子卡在哪、为什么，然后让他真正学会**。为此在原有
「接入层→教学层」之间补一个**学习者模型 / 诊断层**，把零散错误证据变成可教学的理解。

## 分层（记忆 ≠ 理解 ≠ 教会）

```
接入层(证据)        学校消息 / 作业 / 错题照片(VL标注) / 考试 / 听写 / 老师反馈  → 学习记录
                                          │
学习者模型/诊断层 ③  按知识点·错误类型汇总 → 「卡在哪个点、哪类错、可能什么误解」(family_diagnosis)
                                          │            ↑ 可纠正长期记忆 R26(双时态) 保存判断与演化
教学层 ④           据诊断做**面向孩子的苏格拉底式引导**(复用 family_guided)：不给答案，引他自己发现
                                          │
验证层 ⑤           间隔后同类新题**复测**，独立做对才算学会(FSRS 排期，推广自单词间隔复测)
                                          └──────────→ 回填诊断/记忆；没学会→换讲法
```

一切仍为"待家长核对的草稿，确认后才成事实"；不猜答案、不判永久掌握、不输出其他孩子信息。

## 已建（本轮，③ 诊断层 = 核心keystone）

`family_diagnosis.py`：
- **确定性汇总**该孩子该科目的错误证据（错题/考试/同科目进展），带稳定 `record:<id>` 引用。
- **模型只做反思**：据证据命名知识点(knowledge component)、错误类型、可能误解，只引用输入里出现过的 ref；
  证据不足记为「待验证/待核对」。确定性代码校验：引用必须真实、字段有界、状态合法、不编造。
- **留存历史**：`diagnoses` 表按孩子×科目留存，新诊断置旧为 superseded，可回看理解如何变化。
- 端点 `POST /api/diagnosis/run`。真实豆包验证：合成两位数进位加法错题 → 正确诊断"个位满十进一"未稳固、
  错误类型"进位漏加/数位拼接"、给出摆小棒进位的引导，且只引真实记录、诚实列出不确定项。

## 借鉴的开源 / 研究（合适、合理，不盲目引重依赖）

- **贝叶斯知识追踪 BKT / [pyBKT](https://github.com/CAHLR/pyBKT)（CAHLR）**：按知识点累积掌握信号、以对/错更新。
  本轮取其**思想**做轻量确定性版本（错误次数/最近一次/其后是否独立订正）；不引 pyBKT 重依赖，也不在缺乏
  固定知识点体系(R17，待教材数据)时假装算 p(known)。业界 2026 常见架构即「LLM 负责讲、追踪器负责选题」。
- **生成式 Agent 的反思**（Park et al. 2023, Generative Agents）：把观测流综合成更高层判断——诊断即反思。
- **[FSRS](https://github.com/open-spaced-repetition)**（Anki 现代记忆模型）：⑤ 间隔复测排期，推广自现有单词"满7天间隔复测"。
- **苏格拉底式辅导**：[SocraticLM/SocraticMath](https://github.com/ECNU-ICALK/SocraticMath)、MIT AskTIM 的
  review→heuristic→rectify→summarize、"never give the answer"。④ 复用本仓库已有 `family_guided.py`（引导式提示，
  带原作答与有界提示），套用该对话结构，而非新造。
- **可纠正长期记忆**：Zep/Graphiti 的双时态(valid/invalid)已在 R26 `family_learner_memory` 落地，诊断判断可挂其上。

## 已建（④ 引导讲解播种，本轮）

`family_remediation.py`：从一道**已核对的错题**开一个「短引导」草稿——题面作题目、可见订正作家长参考、
关联回原错题、`practice_relation=同一道题`，`shared=False`/`reference_checked=False`（家长在既有短引导界面
核对参考再分享）。**不新造对话引擎**：孩子随后在 `family_guided` 里重做自己的错题，而 `family_guided` 的
提示词本就答案安全（"不给可直接抄交的完整答案、只给一小步提示或一个问题、不宣布掌握"），正是苏格拉底式；
是否独立由既有的提示计数/帮助程度体现。端点 `POST /api/wrong/remediate`；测试 `test_remediation.py`。

## 已建（⑤ 间隔复测 v1，本轮）
诊断时给每个「有支持」的薄弱知识点排复测日（review_on=今天+7，沿用单词满7天复测的确定性做法）；due_reviews(child_id,now) 列出到期该复测的知识点，提醒家长用一道**同类新题**看孩子能否独立做对。验证靠**再诊断**：④引导+一道新题后补一条作答记录、重跑③诊断，该知识点不再「有支持」（或转「有反证」）即改善，全程确定性、不猜掌握。**增强（待R17教材体系）**：知识点有稳定坐标后换 FSRS 按遗忘曲线自适应排期、BKT 更新掌握概率；同类新题仍以家长/老师提供真实材料为准，不臆造。
- **③ 增强**：待 Hermes 的 VL 给错题加"知识点/错误类型候选标签"后，诊断从家长核对的标签起步、减少冷启动；
  待教材单元(R17)到位，知识点对齐课程体系，BKT 掌握度才有稳定坐标。

## 已建（网页入口与展示，r81，已合入 main）

此前③④⑤只有接口与Agent提醒，家长在网页上用不到（诊断永远为空，到期提醒只有“已看过”）。本轮只加入口与只读展示，不新建表、不新增写入路径：

- `family_diagnosis.overview(app, child_id, now)`：确定性、只读，不调模型、不建表。按科目列出错题数与当前诊断；被引 `record:<id>` 按当前归属重新核对（改归其他孩子即“暂不可核对”，不回显旧标题）；存储的证据窗口与当前不同即 `evidence_changed`（待重新诊断）；给出到期复测及应在其下记“复测”的原错题（优先最近被引错题）。经 `/api/goals` 的 `diagnosis` 下发，读取失败只降级本区。
- 画像“错题诊断 · 卡在哪”：诊断/重新诊断（家长触发，复用 `POST /api/diagnosis/run`）、④“让孩子重做这道错题”（复用 `POST /api/wrong/remediate`；同一错题分享前只保留一份草稿，避免重复）、⑤“记录复测结果”（复用既有订正/复测跟进表单，`followup_kind=复测` 关联原错题）。
- ⑤ 判定依据前移到证据：诊断输入里的订正/复测带 `assistance`、`practice_relation`、`related`；提示词明确只有“独立尝试＋相近的新题”做对才是改善证据（订正、看讲解后做对、同题重做都不算）。仍由再诊断判断，不在代码里猜掌握。
- Agent 到期提醒带诊断时的依据与原错题 `record_id`，在“助手跟进”可直接记录复测或跳到画像诊断区；再诊断后不再“有支持”即在下一轮撤回。

下一步：Agent 在证据变化后后台自动重跑诊断（受每轮模型预算约束，R31“打开即读准备好的结果”），目前仍由家长点按钮；真实错题与孩子重做效果待家庭验收。

## 与第二开发(Hermes)的边界
Hermes 负责①把错题**读准标全**(含知识点/错误类型候选标签)；本侧负责②入库(已保留他的实现)与③④⑤理解教学闭环。
诊断层以 `record:<id>` 引用为契约，只读记录、不改其保存语义。
