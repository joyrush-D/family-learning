# 项目工作约定

- 当前处于家庭场景验证与开源准备阶段；不要把规划功能描述为已实现。
- 工作前阅读README.md；执行本机家庭跟踪任务时还需阅读家庭运行规则.md、消息来源.md、跟踪台账.md、学习与成长.md。
- 真实家庭记录必须保留在本地、保持Git忽略；不得用真实孩子信息生成公开示例、测试数据、截图、issue或PR描述。
- .gitignore为公开文件白名单。新增程序或文档时，仅放行明确的公开路径；不要放行数据、导出、附件、日志、环境变量或本地账号配置。
- 发布前检查暂存文件内容和完整历史；Git忽略不是加密，也不能清除已经提交的内容。
- 本项目MIT许可证仅适用于拥有许可权的项目内容；复用第三方代码先核对其许可证。
- 优先复用现有记录与流程，手动输入和文件导入应能独立工作，再接消息来源；不为未知需求添加框架或服务。
- 产品目标与优先级以README.md、V1交付目标.md为基线：家长省事、孩子有可核对的成长；学校作业/课程/事务优先。用户反馈先追溯完整使用流程与共同原因，不逐条叠加按钮、入口或框架，不把实现缺陷当成需求变化。
- 每轮开发、续接或反馈跟进先读V1交付目标.md的“需求与方案总账”和“实现路线与切换条件”。需求不因某条路线失败、权限等待、评审超时、换会话或收窄当前迭代而删除。按需求编号维护当前执行路线、主选/备选、阻碍、下一步和验收证据；已授权且条件满足的备选继续验证，不能把局部等待当作整个产品无事可做。只有用户明确撤销或替换需求才调整范围，保留原因和历史。阶段结束前回查全部未完成项，不能只看本轮新反馈。
- UI或流程变更须先把已有家长反馈对应到具体屏幕、操作与期望，复现后同步相关浏览器验收和使用说明；验收实际入口、完整操作、保存后重开、失败重试及手机/桌面布局。旧页面脚本、截图或局部测试通过不能代替当前流程通过；不得靠删除业务断言掩盖退步。检查未通过时修复或回退本次变更，保留原记录；家长尚未实际验证的体验问题保持待验收，不将软件通过写成家长满意。
- 外部评审提供明确问题、准确文件/截图、脱敏材料与验收标准，限定范围和轮数；核对实际模型、已读取材料与评审结论，逐项记录采纳或不采纳的理由。启动调用不是完成评审，评审意见不能代替实测。
- 常规实现、缺陷修复、文案样式、测试及已授权部署发布由执行者负责。仅在既有指令不足以解决互斥产品取舍、新增费用或数据授权、不可逆影响时请用户仲裁，并给出具体选项、推荐及影响；不重复询问已确定目标或已授权操作。

- GitHub `main` 是唯一长期公开源码主线。新工作从最新 main 开一个短期 `codex/<任务>` 分支，一个可独立验收的改动一个 PR；验证后及时合入，再从最新 main 开始下一项，不从未合入的功能分支连续叠新任务，不另建长期 develop 或工程师专属主线。
- 合入保留提交归属，优先快进或 PR rebase；不强推、不改写共享历史。清理分支前确认其提交已包含在 main、没有仍需保留的工作；删除远端分支时核对当前头未变化。未合入代码和他人未提交修改不可清除。
- 交付同步维护 README 的当前版本与能力边界、V1 需求总账及受影响的 PRD/运行说明；定位变化时更新 GitHub About，不能把功能分支或已推送源码描述为家庭已部署。家庭私有工作目录的历史不能直接推到公开仓库，部署继续保护唯一主库。

## 多 Agent 协作（Codex · Claude · Hermes）

三个开发 Agent 共用本仓库和这份规则，彼此看不到对方的对话，只通过 `main`、[协作看板](协作看板.md)和交接文档衔接。用户确定产品目标与关键取舍；Codex据此维护优先级、任务分工与依赖，保留用户已指定的负责人。开工前读本文件、看板中自己名下的行和 V1 总账对应需求，只做自己名下的任务；发现别人范围内的问题，写进看板而不是顺手修改。

### 整体责任与仲裁

- Codex负责整体产品把握和交付统筹：维护完整目标、需求总账、优先级与协作看板，跟进跨Agent依赖、阻碍和遗漏，向用户提供统一进展；常规协作不要求用户反复传话。
- 例行统筹检查共享看板、分支/PR、检查结果和家庭版本；有条件满足的工作即明确负责人、依赖和验收标准，常规技术接口由Codex协调。已分配不等于已启动：有可执行项时须实际派发到执行方CLI或确认其已领取，并核对模型响应、工具调用和工作分支；不能只写看板后等提交。CLI退出码为0也须核对模型错误与交付产物；失败保留状态并有界续接。执行方已有活跃任务时只跟踪，不重复启动。开工状态由执行者或核验实际启动的Codex回填。通过门槛后推进合入、本机验证与已授权家庭发布；无变化不重复全套检查或重启服务。
- Codex对最终交付核验负责：分别确认代码已合入、家庭已部署、真实验收结果和未完成条件。Claude合入或Hermes专项通过不能代替整体交付；缺少真实证据的项目继续保留。
- 各Agent继续在自己的模块和看板任务内执行；Codex统筹不意味着代改其他人的功能。分工或优先级调整须在看板记录原因，不能擅自撤销用户确定的需求、暂停决定或验收要求。
- 用户保留最终决策权。仅既有指令无法解决的互斥产品取舍、新增费用或数据授权、不可逆影响提交仲裁，并给出推荐方案与影响；日常实现、测试、协调和已授权发布由负责Agent执行。

### Codex · 家庭运行 Mac（整体统筹、集成与现场）
- 唯一接触真实家庭实例、真实数据、消息来源和家用设备的 Agent。
- 负责：按[家庭实例升级与回退](AGENT运行.md#家庭实例升级与回退)发布（备份、隔离恢复、升级、读回）；按[待验证清单](待验证清单.md)做真实验收并回填；真实家庭反馈引出的现场修复；微信/QQ 等来源接入与故障；家庭部署记录（rN）。
- 发布前复核将要部署的改动，重点是儿童可见内容不泄露答案、不跨孩子、不外发家庭资料；发现问题可直接小修并记录原因。
- 不在家庭实例上做实验性开发或造测试数据；不把真实家庭内容写进公开文件。

### Claude · 开发 Mac（核心系统、中枢逻辑与合入）

- 作为核心系统技术负责人，统一负责记忆系统、教学目标拆解和独立Family Agent的核心决策闭环；这是开发职责，产品运行不依赖Claude开发会话或绑定Claude模型。
- 记忆系统：区分事实、家长/孩子转述、待验证假设、已确认判断、方法接受度与效果；保留出处、时间、适用范围、更正/失效和历史，决定本轮应取哪些证据及哪些仍未知。
- 教学目标拆解：结合学校要求、课程/年级目标和孩子实际证据，形成可核对的阶段小目标与本次任务，写清完成表现、核对方式和复测条件；尊重学校作业、当日时间与休息约束，优先复用已有材料/App/设备，正式计划经家长确认。
- 中枢逻辑：证据进入→更新理解→诊断与拆解→计划草稿→家长确认→执行反馈→复测→调整；负责触发条件、去重、失败恢复、模型可替换与按任务控制用量。复用现有模块，不因称为“中枢”就新增服务、框架或第二套记忆库。
- 主要维护`family_agent.py`、`family_goals.py`、`family_learner_memory.py`、`family_diagnosis.py`、`family_remediation.py`、`family_guided.py`及相应交互；共享模型/应用接口改动先与Codex协调，Hermes的视觉模块仍由Hermes负责。功能分支合入前完整回归，按协作流程快进合入`main`。
- 只用隔离虚构家庭和已授权模型做合成验证，接触不到真实家庭数据。需要真实环境才能确认的内容写进待验证清单交给Codex，不以虚构检查冒充真实验收；真实接入、部署和最终交付统筹仍由Codex负责。

### Hermes · 视觉与模型专项
- 负责：错题照片“读准标全”（`family_wrong_questions.py`、`family_wrong_review.py`、`wrong-review.*`）：真实样卷精度、手写框、密集/多栏/跨页、知识点/错误类型候选标签、跨模型对比。分工与接口见[错题线交接](交接-错题VL-第二开发.md)。
- 不改诊断、重做、复测、学习目标和画像；需要它们配合时在看板登记，由负责方实现。

### 共同流程
1. 开工：`git fetch` 后从最新 `main` 开 `codex/<任务>` 短分支，在看板自己的行写上分支名和状态。
2. 修改范围：只改自己负责的模块。`app.py`、`app.js`、`.gitignore`、README、PRD、V1 总账、待验证清单等公共文件只追加本任务相关段落；PRD 版本、V1 范围行和 rN 由合入方统一更新。同一文件同一时间只由一个 Agent 修改，冲突时在看板排先后。
3. 跨 Agent 接口先写进交接文档再实现；改接口先改文档，并在看板通知负责方。
4. 交付：分支内跑相关 Python 与 360/1440 浏览器检查，写明做了什么、怎么验、边界；需现场验证的登记到待验证清单。
5. 合入：功能分支默认由 Claude 回归后快进合入；Codex 的发布记录与现场修复可直接快进推送。推送前先 `git fetch`，确认 `origin/main` 是本分支祖先；不强推、不改写共享历史。合入后更新看板。
6. 发布：只有 Codex 部署家庭实例。合入、部署、家庭验收分开记录，前一步完成不代表后一步完成。
7. 追溯：提交用本 Agent 的作者名或 `Co-Authored-By` 标明来源，看板保留分支名，便于回查。

### 例行巡检（执行方）
Codex 的例行统筹负责分配与推进；Claude、Hermes（以及 Codex 执行自己名下任务时）按同一步骤定时巡检，不靠用户传话：
1. 同步：`git fetch`，本地 `main` 快进到 `origin/main`；本地有未推送提交或未提交修改时不丢弃，写进看板后结束本次巡检。读本文件与[协作看板](协作看板.md)。
2. 领取：先检查本机现有会话/工作树是否已执行该项；活跃任务只跟踪，已有修改先衔接，不重复领取。只取看板上负责人是自己、状态为“待开始 / 进行中 / 受阻”且依赖已满足的第一项；开工时在该行写上分支名并改为“进行中”，推送看板，表示已领取。
3. 做一小步可交付的工作：按“共同流程”实现、测试和交付；一次巡检做不完，把进度与下一步写回该行，只推送分支，不合入半成品。
4. 回填：在看板写明状态、分支和证据位置；受阻写明原因与解除条件。
5. 空闲：没有可做的任务就在“空闲与待分配”登记；当天已有同样的登记就不重复，直接结束。
6. 无人值守时，需要用户决定、真实家庭数据或设备、新增费用或不可逆影响的事一律不做，写进看板等 Codex 或用户处理；不强推、不改写共享历史、不删除分支、不改他人负责的模块。

巡检时间（北京时间）：Claude 由开发 Mac 的定时任务在 10:00、15:00、21:00 运行；Hermes 的定时由用户在其运行环境设置；Codex 的例行统筹见看板“例行统筹”。
