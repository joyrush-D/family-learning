# 交接：错题这条线的分工（第二开发 ↔ 主执行）

目标闭环：错题照片 → ①读准并标注 → ②核对入库 → ③按知识点/错误类型诊断 → ④面向孩子的引导讲解、让他重做 → ⑤间隔后同类新题复测，确认独立学会 → 回填记忆、没学会就换讲法。

**分工**：第二开发负责 ①（把错题读准、标全、可诊断）；主执行负责 ②③④⑤（核对入库 + 理解/让孩子跟上的闭环）。接口约定见下，避免撞车。

## 第二开发继续做（①：读准、标全、可诊断）

1. **真实样卷精度**：儿童手写、涂改、光线不均、手机倾斜下，`wrong_item` 框是否贴合、`text/answer/correction` 是否逐字正确；`uncertain`/`uncertainties` 是否如实标注看不清处。先用非隐私样卷或家长同意的照片。
2. **补齐 `handwriting` 框**：当前合成案例里把学生作答并进了 `wrong_item` 整题框，没单独出手写区。诊断④要用到"解答过程/草稿"的独立区域，请让模型对手写解答单独出 `handwriting` 框（题面/作答/批改仍在 `wrong_item`）。
3. **密集/多栏/跨页**：多题密集排版、两栏试卷、跨页大题时，`box` 坐标不要串行或错位；跨页大题归属不清写入 `uncertainties`。
4. **每个 `wrong_item` 增加"知识点/错误类型"候选标签（新字段，可选、默认空、待家长核对）**：这是③诊断层的输入接口。建议在 region 里加两个字段：
   - `topic_hint`：知识点候选（如"两位数进位加法""ea/ee 拼写""比喻句"），看不清或没把握留空。
   - `error_hint`：错误类型候选（如"进位漏加""审题漏条件""形近字混淆"），留空亦可。
   两者都是**草稿提示**，绝不当作事实；主执行在②核对时展示给家长确认，③按它聚合。字段命名/结构若要调整，改前同步一下，别改已有 `kind/box/label/text/answer/correction/uncertain` 的语义。
5. **OpenRouter 免费 VL 对比**（`inclusionai/ling-3.0-flash-vl:free`）：用同一组合成案例对比框选/转写质量、延迟、失败率；不达标不切家庭生产模型。

**不要动**：网页录入、原件关联、学习记录保存、目标/画像——这些是②③④⑤，归主执行，避免与他的入库改动冲突。

## 主执行负责（②③④⑤）

- ②（进行中，分支 `codex/wrong-question-review`，基于本分支）：网页上传错题照片 → 调 `family_wrong_questions.annotate_pages` 出草稿 → 同屏在照片上画框、逐题核对/编辑 → 保存为错题学习记录、关联原件、可挂到对应学习目标。复用现有作业照片→草稿→记录与上传/附件流程。
- ③诊断层：按 `topic_hint/error_hint`（家长核对后）聚合错题+考试+听写，输出"卡在哪个知识点、什么错法"，进画像与再评估。
- ④面向孩子的引导：对一道错题给苏格拉底式引导（不直接给答案），让孩子重做，记录是否独立完成。
- ⑤确认理解：把"间隔后独立复测"从单词推广到知识点——隔数日用同类新题复测，独立做对才算学会。

## 接口稳定性约定
- `annotate_pages(images, subject_hint, ...) -> {pages:[{page, regions:[{kind,box,label,text,answer,correction,uncertain, topic_hint?, error_hint?}]}], uncertainties}` 这个返回结构是②的输入契约，改字段请先同步。
- 坐标系固定 0..1000、左上原点；`wrong_item/handwriting/layout` 三类语义不变。
- 一切仍为"待核对草稿，家长保存后才成事实"，不写家庭库、不猜答案、不判掌握——两边都守这条。
